version: '3.8'

services:
  # The main Rust telemetry generator application
  telemetry-app:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    networks:
      - telemetry-network
    restart: unless-stopped

  # Prometheus for metrics scraping and aggregation
  prometheus:
    image: prom/prometheus:latest
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
    ports:
      - "9090:9090"
    networks:
      - telemetry-network
    depends_on:
      - telemetry-app
    restart: unless-stopped

  # Grafana for real-time observability and dashboards
  grafana:
    image: grafana/grafana:latest
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
    networks:
      - telemetry-network
    depends_on:
      - prometheus
    restart: unless-stopped

networks:
  # Custom bridge network to allow services to communicate via hostname
  telemetry-network:
    driver: bridge