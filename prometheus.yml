global:
  scrape_interval: 5s 
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'rust_telemetry'
    
    metrics_path: '/metrics'
    
    static_configs:
      - targets: ['telemetry-app:8080']